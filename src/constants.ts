// Shared identifier used across the extension for the remote authority scheme, SSH host
// aliases, and on-disk markers. Must match the id used in package.json (commands and
// `onResolveRemoteAuthority`).
export const EXTENSION_ID = "dev-containers-oss";
